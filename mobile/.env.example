# Base URL of the TendX web API the mobile app shares (Build Spec section 6.9).
# Use your machine's LAN IP (not localhost) when testing on a physical device,
# e.g. http://192.168.1.20:3000, so the phone can reach the dev server.
EXPO_PUBLIC_API_URL=http://localhost:3000
